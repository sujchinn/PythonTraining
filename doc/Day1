getting Help

>>> help(str)
Help on class str in module __builtin__:

class str(basestring)
 |  str(object='') -> string                                 #-> means implements base class string
 |
 |  Return a nice string representation of the object.
 |  If the argument is a string, the return value is the same object.
 |
 |

>>> help(type)
Help on class type in module __builtin__:

class type(object)
 |  type(object) -> the object's type
 |  type(name, bases, dict) -> a new type
 |
 |  Methods defined here:

>>> help(str.upper)
Help on method_descriptor:

upper(...)
    S.upper() -> string

    Return a copy of the string S converted to uppercase.

IDLE:
Help-->Python Docs (F1)

__built_in__ - is the base calss for all the builtin functions.

Libraries:
---------
import time
---imports time library
dir(time)
----lists all the builtin function of time lib
help(time)
----helps only after importing time lib


for loop
--------
for i in xrange(1,5)
or
for i in range(1,5)

xrange - it remembers only the current iteration value
range - it remembers all the values in the range


>>> range(6)
[0, 1, 2, 3, 4, 5]
>>> xrange(6) #operates only in for loop
xrange(6)
>>> list(xrange(6))
[0, 1, 2, 3, 4, 5]
>>>

for index in xrange(11):
    print index

for index in xrange(len(name)):
    print name[index]
or
for element in name:
    print element


Tuples
------

arr = (10,20,30,40,50) - tuple
immutable

arr1=[10,20,30] - list
mutable

arr1.append(40)
arr1.remove(20)
arr1.pop() #last element
arr.pop(0)
del arr(1)
del arr


strings are immutable


